import * as utilityFunctions from '../util/octokit-utils.mjs';

// Todo: implement
describe('utility functions', () => {
    it('compareLastInfosWithCurrentSoftwareDepsList',() => {
        // utilityFunctions.compareLastInfosWithCurrentSoftwareDepsList(null, null);
    });

    it('isUpdateAvailable',() => {
        // utilityFunctions.isUpdateAvailable(null);
    });

    it('prepareDataForNotification',() => {
        // utilityFunctions.prepareDataForNotification(null);
    });

    it('updateData',() => {
        // utilityFunctions.updateData(null);
    });
});
